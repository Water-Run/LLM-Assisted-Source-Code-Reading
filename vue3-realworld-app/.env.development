# ============================================================
# 文件功能概述:
# 这个文件用于配置开发环境下的环境变量
# 开发环境是指程序员正在编写和测试代码时使用的环境
# Vite在运行开发服务器时会自动加载这个文件
# ============================================================

# 环境变量名称以VITE_开头，这是Vite项目的规范
# 这样命名可以确保这些变量能够被客户端代码访问
# 如果不以VITE_开头，变量默认是服务端的，客户端无法使用

# VITE_BASE_URL: 定义API请求的基础URL地址
# 这里设置为空字符串，意味着在开发环境下
# API请求会使用相对路径，通常指向本地的开发服务器
# 例如: http://localhost:3000/api/xxx
VITE_BASE_URL=""